import { Routes } from '@angular/router';
import { BehaviorComponent } from './behavior.component';

export const BEHAVIOR_ROUTES: Routes = [
  {
    path: '',
    component: BehaviorComponent,
  },
];
